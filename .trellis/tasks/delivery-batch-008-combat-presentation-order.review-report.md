# 双阶段评审报告

## Stage 1: 规范符合

- AC-001/002：通过。状态事务各自只发送一次 `changed`，检查点证明结算位于起手之后。
- AC-003：通过。卡牌点击与拖拽、药水、结束回合和快捷键均检查统一忙碌状态。
- AC-004：通过。票据校验阻止过期协程继续，`_exit_tree()` 使未完成票据失效。
- AC-005：通过。数据与数值文件无修改。

## Stage 2: 代码质量

- 演出编排保持在 `Main`，战斗规则仍由 `CombatState` 持有，没有把视觉计时写入数据层。
- headless 同步路径保留原测试和自动化模拟速度；正式运行采用局部短延迟，不修改全局 `Engine.time_scale`。
- 新增独立回归测试，且原拖拽、遥测、热键、视觉边界测试继续通过。

## 裁决

Critical 0 / Major 0 / Minor 0，允许进入图库验收、提交和 Alpha 打包。

## 残余风险

- 当前敌方动作仍以回合开始前的公开意图作为起手表现来源；极少数由回合开始灼烧触发 Boss 阶段切换的情况，后续应把“敌方行动准备”拆成状态层显式事务，以保证阶段切换后的实际行动载荷与起手表现完全一致。
