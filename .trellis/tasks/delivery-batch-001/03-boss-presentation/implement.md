# 三章 Boss 表现实现记录

## 已实现

- 新增风暴执政官和回路核心正式透明立绘。
- `ENEMY_ART_PATHS` 接入三章 Boss 独立位图，并配置舞台缩放。
- 新增 `boss_phase_profiles`，覆盖三章 Boss 共 6 个阶段画像。
- 阶段冲击颜色、射线预算、震屏、顿帧和角色缩放改为按 Boss/阶段读取。
- 新增 `phase_forge`、`phase_storm`、`phase_nexus` 音频事件映射，当前复用稳定阶段音频素材并保留后续独立替换入口。
- 补充音频、流程和数据完整性测试。

## 约束

- 未修改 Boss 数值、阶段阈值、行动或奖励。
- 阶段画像缺失时继续回退到原通用 `impact_phase` 表现。
